const getProjectedSnapsRemaining = ({
  quarter,
  clockTime,
  down,
  yardsToGo
}) => {
  // count plays
  // calculate time elapsed
  // calculate play rate
  // projected remaining plays (remaining time / play rate)
}

export default getProjectedSnapsRemaining
