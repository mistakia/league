{
  "name": "should handle player_air_yards_share_from_plays",
  "description": "Test case for should handle player_air_yards_share_from_plays",
  "request": {
    "prefix_columns": ["player_name"],
    "columns": [
      {
        "column_id": "player_air_yards_share_from_plays",
        "params": { "year": [2023] }
      }
    ],
    "where": [
      {
        "column_id": "player_air_yards_share_from_plays",
        "operator": ">=",
        "value": 25,
        "params": { "year": [2023] }
      }
    ],
    "sort": [{ "column_id": "player_air_yards_share_from_plays", "desc": true }]
  },
  "expected_query": "with \"td2a52d569d09f5ed53e240c30d08ae2b\" as materialized (select \"pg\".\"pid\", CASE WHEN SUM(nfl_plays.depth_of_target) > 0 THEN ROUND(100.0 * SUM(CASE WHEN nfl_plays.target_pid = pg.pid THEN nfl_plays.depth_of_target ELSE 0 END) / NULLIF(SUM(nfl_plays.depth_of_target), 0), 2) ELSE NULL END as air_yds_share_from_plays from \"nfl_plays\" inner join \"player_gamelogs\" as \"pg\" on \"nfl_plays\".\"esbid\" = \"pg\".\"esbid\" and \"nfl_plays\".\"offense_nfl_team\" = \"pg\".\"nfl_team\" where not \"play_type\" = 'NOPL' and (\"target_pid\" is not null) and \"nfl_plays\".\"season_type\" in ('REG') and \"nfl_plays\".\"season_year\" in (2023) and \"pg\".\"season_year\" in (2023) group by \"pg\".\"pid\" having CASE WHEN SUM(nfl_plays.depth_of_target) > 0 THEN ROUND(100.0 * SUM(CASE WHEN nfl_plays.target_pid = pg.pid THEN nfl_plays.depth_of_target ELSE 0 END) / NULLIF(SUM(nfl_plays.depth_of_target), 0), 2) ELSE NULL END >= '25'), \"base_years\" as (SELECT unnest(ARRAY[2023]) as year), \"player_years\" as (SELECT DISTINCT player.pid, base_years.year FROM player CROSS JOIN base_years) select \"player\".\"pid\", \"td2a52d569d09f5ed53e240c30d08ae2b\".\"air_yds_share_from_plays\" AS \"air_yds_share_from_plays_0\", player.first_name, player.last_name, \"player\".\"primary_position\" from \"player\" inner join \"player_years\" on \"player_years\".\"pid\" = \"player\".\"pid\" inner join \"td2a52d569d09f5ed53e240c30d08ae2b\" on \"td2a52d569d09f5ed53e240c30d08ae2b\".\"pid\" = \"player\".\"pid\" group by \"td2a52d569d09f5ed53e240c30d08ae2b\".\"air_yds_share_from_plays\", player.first_name, player.last_name, \"player\".\"pid\", \"player\".\"last_name\", \"player\".\"first_name\", \"player\".\"primary_position\" order by 2 DESC NULLS LAST, \"player\".\"pid\" asc limit 500",
  "tags": ["player", "plays", "filters", "parameters", "sorting", "cte"],
  "timeout_ms": 40000,
  "expected_metadata": { "cache_ttl": "one_week", "cache_expire_at": null }
}
