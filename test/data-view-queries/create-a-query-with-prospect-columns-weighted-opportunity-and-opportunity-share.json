{
  "name": "should create a query with prospect columns, weighted opportunity, and opportunity share",
  "description": "Test case for should create a query with prospect columns, weighted opportunity, and opportunity share",
  "request": {
    "columns": [
      {
        "column_id": "player_weighted_opportunity_from_plays",
        "params": { "year": [2023, 2022, 2021, 2020] }
      },
      { "column_id": "player_opportunity_share_from_plays" },
      { "column_id": "player_body_mass_index" },
      { "column_id": "player_speed_score" },
      { "column_id": "player_height_adjusted_speed_score" },
      { "column_id": "player_agility_score" },
      { "column_id": "player_burst_score" }
    ],
    "sort": [
      { "column_id": "player_weighted_opportunity_from_plays", "desc": true }
    ]
  },
  "expected_query": "with \"t17dc711209fe80a29714bb91146be980\" as materialized (select COALESCE(ball_carrier_pid, target_pid) as pid, ROUND(SUM(CASE WHEN nfl_plays.yard_line_100 <= 20 AND ball_carrier_pid IS NOT NULL THEN 1.30 WHEN nfl_plays.yard_line_100 <= 20 AND target_pid IS NOT NULL THEN 2.25 WHEN nfl_plays.yard_line_100 > 20 AND ball_carrier_pid IS NOT NULL THEN 0.48 WHEN nfl_plays.yard_line_100 > 20 AND target_pid IS NOT NULL THEN 1.43 ELSE 0 END), 2) as weighted_opportunity_from_plays from \"nfl_plays\" where not \"play_type\" = 'NOPL' and \"nfl_plays\".\"season_type\" in ('REG') and \"nfl_plays\".\"season_year\" in (2020, 2021, 2022, 2023) group by COALESCE(ball_carrier_pid, target_pid)), \"base_years\" as (SELECT unnest(ARRAY[2020,2021,2022,2023]) as year), \"player_years\" as (SELECT DISTINCT player.pid, base_years.year FROM player CROSS JOIN base_years), \"t0e6a2391a9183526a5157033ce248276\" as materialized (select \"pg\".\"pid\", ROUND(100.0 * (COUNT(CASE WHEN nfl_plays.ball_carrier_pid = pg.pid THEN 1 ELSE NULL END) + COUNT(CASE WHEN nfl_plays.target_pid = pg.pid THEN 1 ELSE NULL END)) / NULLIF(SUM(CASE WHEN nfl_plays.ball_carrier_pid IS NOT NULL OR nfl_plays.target_pid IS NOT NULL THEN 1 ELSE 0 END), 0), 2) as opportunity_share_from_plays from \"nfl_plays\" inner join \"player_gamelogs\" as \"pg\" on \"nfl_plays\".\"esbid\" = \"pg\".\"esbid\" and \"nfl_plays\".\"offense_nfl_team\" = \"pg\".\"nfl_team\" where not \"play_type\" = 'NOPL' and (\"ball_carrier_pid\" is not null or \"target_pid\" is not null) and \"nfl_plays\".\"season_type\" in ('REG') and \"nfl_plays\".\"season_year\" in (2020, 2021, 2022, 2023) group by \"pg\".\"pid\") select \"player\".\"pid\", \"t17dc711209fe80a29714bb91146be980\".\"weighted_opportunity_from_plays\" AS \"weighted_opportunity_from_plays_0\", \"t0e6a2391a9183526a5157033ce248276\".\"opportunity_share_from_plays\" AS \"opportunity_share_from_plays_0\", CASE WHEN player.height_inches > 0 THEN ROUND(CAST((player.weight_pounds::float / NULLIF(player.height_inches::float * player.height_inches::float, 0)) * 703 AS NUMERIC), 2) ELSE NULL END as bmi_0, CASE WHEN player.forty_yard_dash_seconds > 0 THEN ROUND((player.weight_pounds * 200.0) / NULLIF(POWER(player.forty_yard_dash_seconds, 4), 0), 2) ELSE NULL END as speed_score_0, CASE WHEN player.primary_position IN ('WR', 'TE') AND player.forty_yard_dash_seconds > 0 THEN ROUND(((player.weight_pounds * 200.0) / NULLIF(POWER(player.forty_yard_dash_seconds, 4), 0)) * (player.height_inches / CASE WHEN player.primary_position = 'TE' THEN 76.4 ELSE 73.0 END), 2) ELSE NULL END as height_adjusted_speed_score_0, ROUND(COALESCE(player.shuttle_run_seconds, 0) + COALESCE(player.three_cone_drill_seconds, 0), 2) as agility_score_0, ROUND(COALESCE(player.vertical_jump_inches, 0) + (COALESCE(player.broad_jump_inches, 0) / 12.0), 2) as burst_score_0, \"player\".\"primary_position\" from \"player\" inner join \"player_years\" on \"player_years\".\"pid\" = \"player\".\"pid\" left join \"t17dc711209fe80a29714bb91146be980\" on \"t17dc711209fe80a29714bb91146be980\".\"pid\" = \"player\".\"pid\" left join \"t0e6a2391a9183526a5157033ce248276\" on \"t0e6a2391a9183526a5157033ce248276\".\"pid\" = \"player\".\"pid\" group by \"t17dc711209fe80a29714bb91146be980\".\"weighted_opportunity_from_plays\", \"t0e6a2391a9183526a5157033ce248276\".\"opportunity_share_from_plays\", player.weight_pounds, player.height_inches, player.weight_pounds, player.forty_yard_dash_seconds, player.weight_pounds, player.forty_yard_dash_seconds, player.height_inches, player.primary_position, player.shuttle_run_seconds, player.three_cone_drill_seconds, player.vertical_jump_inches, player.broad_jump_inches, \"player\".\"pid\", \"player\".\"last_name\", \"player\".\"first_name\", \"player\".\"primary_position\" order by 2 DESC NULLS LAST, \"player\".\"pid\" asc limit 500",
  "tags": ["parameters", "sorting", "cte"],
  "timeout_ms": 40000,
  "expected_metadata": { "cache_ttl": "six_hours", "cache_expire_at": null }
}
